# Bug Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Correggere 11 bug funzionali e di qualità trovati nell'analisi del codice di CallsBlocker, con test per ognuno.

**Architecture:** Ogni fix è indipendente e auto-contenuto. L'unica modifica architetturale è lo spostamento del `MainViewModel` a scope di Activity (condiviso tra tutte le schermate) per risolvere il problema dello stato stale di `isScreeningActive`. Le altre fix sono locali al file interessato.

**Tech Stack:** Kotlin 2.1 · Jetpack Compose · Hilt · Room · Robolectric · MockK · Compose Testing (instrumented, API 29+)

**Test runner unit:** `.\gradlew.bat test`  
**Test runner instrumented:** `.\gradlew.bat connectedAndroidTest` (richiede emulatore/device API 29+)  
**Build check:** `.\gradlew.bat assembleDebug`

---

## Bug Inventory (con priorità)

| # | Bug | File | Severità |
|---|-----|------|----------|
| 1 | Toggle notifiche mostra stato sbagliato all'apertura | `SettingsScreen.kt` | 🔴 Alta |
| 2 | `isScreeningActive` non si aggiorna tornando alla Home | `MainActivity.kt`, `HomeScreen.kt` | 🔴 Alta |
| 3 | Icona notifica è il placeholder di sistema | `NotificationHelper.kt` | 🟠 Media |
| 4 | Nessun delete singolo nei log chiamate | DAO, Repository, ViewModel, `BlockedCallsScreen.kt` | 🟠 Media |
| 5 | InfoScreen senza pulsante indietro | `InfoScreen.kt` | 🟠 Media |
| 6 | Toast dalla ViewModel (anti-pattern) | `MainViewModel.kt`, `HomeScreen.kt` | 🟠 Media |
| 7 | `deleteEntry` fa O(n) scan in memoria | `BlockedEntryDao.kt`, `BlockedEntryRepository.kt`, `MainViewModel.kt` | 🟡 Bassa |
| 8 | `SimpleDateFormat` ricreato ad ogni recomposition | `BlockedCallsScreen.kt` | 🟡 Bassa |
| 9 | `ModalBottomSheet` + `imePadding()` = doppio inset tastiera | `EntryBottomSheet.kt` | 🟡 Bassa |
| 10 | `OnboardingViewModel` inietta `context` non usato | `OnboardingViewModel.kt` | 🟡 Bassa |
| 11 | Import inutilizzati in `MainActivity` | `MainActivity.kt` | 🟡 Bassa |

---

## File Map

| File | Azione | Motivo |
|------|--------|--------|
| `data/BlockedEntryDao.kt` | Modify | Aggiungere `deleteById(id)` |
| `data/BlockedCallLogDao.kt` | Modify | Aggiungere `deleteLog(id)` |
| `data/BlockedEntryRepository.kt` | Modify | Esporre `deleteById` e `deleteLog` |
| `ui/MainViewModel.kt` | Modify | Sostituire Toast con `userMessage` state; usare `deleteById`; aggiungere `deleteCallLog` |
| `ui/MainActivity.kt` | Modify | Spostare ViewModel a scope Activity; rimuovere import inutilizzati |
| `ui/screens/HomeScreen.kt` | Modify | Ricevere ViewModel condiviso; mostrare Snackbar da `userMessage` |
| `ui/screens/SettingsScreen.kt` | Modify | Fix toggle notifiche stale |
| `ui/screens/BlockedCallsScreen.kt` | Modify | Aggiungere delete singolo log; fix `SimpleDateFormat` |
| `ui/screens/InfoScreen.kt` | Modify | Aggiungere pulsante indietro |
| `ui/components/EntryBottomSheet.kt` | Modify | Fix doppio inset tastiera |
| `ui/OnboardingViewModel.kt` | Modify | Rimuovere `context` inutilizzato |
| `util/NotificationHelper.kt` | Modify | Fix icona notifica |
| `src/test/.../MainViewModelTest.kt` | Modify | Test per `deleteById`, `deleteCallLog`, `userMessage` |
| `src/androidTest/.../SettingsScreenTest.kt` | Modify | Test toggle notifiche |
| `src/androidTest/.../InfoScreenTest.kt` | Modify | Test pulsante indietro |
| `src/androidTest/.../BlockedCallsScreenTest.kt` | Create | Test delete singolo log |

---

## Task 1 — Fix DAO: `deleteById` in `BlockedEntryDao` + `deleteLog` in `BlockedCallLogDao`

**Files:**
- Modify: `app/src/main/java/com/callsblocker/data/BlockedEntryDao.kt`
- Modify: `app/src/main/java/com/callsblocker/data/BlockedCallLogDao.kt`
- Modify: `app/src/main/java/com/callsblocker/data/BlockedEntryRepository.kt`

- [ ] **Step 1: Aggiungere `deleteById` a `BlockedEntryDao`**

```kotlin
// In BlockedEntryDao.kt, aggiungere dopo la fun delete(entry):
@Query("DELETE FROM blocked_entries WHERE id = :id")
suspend fun deleteById(id: Long)
```

- [ ] **Step 2: Aggiungere `deleteLog` a `BlockedCallLogDao`**

```kotlin
// In BlockedCallLogDao.kt, aggiungere dopo la fun deleteAllLogs():
@Query("DELETE FROM blocked_call_logs WHERE id = :id")
suspend fun deleteLog(id: Long)
```

- [ ] **Step 3: Esporre i nuovi metodi in `BlockedEntryRepository`**

```kotlin
// In BlockedEntryRepository.kt, sostituire il metodo delete esistente e aggiungere:
suspend fun deleteById(id: Long) = blockedEntryDao.deleteById(id)

// Nella sezione Call Logs, aggiungere:
suspend fun deleteLog(id: Long) = blockedCallLogDao.deleteLog(id)
```

- [ ] **Step 4: Build per verificare compilazione**

```
.\gradlew.bat assembleDebug
```
Expected: `BUILD SUCCESSFUL`

- [ ] **Step 5: Commit**

```
git add app/src/main/java/com/callsblocker/data/BlockedEntryDao.kt
git add app/src/main/java/com/callsblocker/data/BlockedCallLogDao.kt
git add app/src/main/java/com/callsblocker/data/BlockedEntryRepository.kt
git commit -m "feat(data): add deleteById to BlockedEntryDao and deleteLog to BlockedCallLogDao"
```

---

## Task 2 — Fix ViewModel: Toast → `userMessage` state + `deleteById` + `deleteCallLog`

**Files:**
- Modify: `app/src/main/java/com/callsblocker/ui/MainViewModel.kt`
- Modify: `app/src/test/java/com/callsblocker/ui/MainViewModelTest.kt`

**Bug 6 (Toast anti-pattern) + Bug 7 (O(n) lookup) + Bug 4 (ViewModel side)**

- [ ] **Step 1: Scrivere i test failing per i nuovi comportamenti**

Aprire `app/src/test/java/com/callsblocker/ui/MainViewModelTest.kt` e aggiungere in fondo alla classe `MainViewModelTest`:

```kotlin
@Test
fun deleteEntry_callsDeleteById_notDeleteWithEntity() = runTest {
    // Arrange: una entry nella lista
    val entry = BlockedEntry(id = 42L, pattern = "333", isPrefix = false, label = "", action = CallAction.BLOCK)
    doReturn(flowOf(listOf(entry))).`when`(repository).getAll()
    doReturn(flowOf(emptyList<BlockedCallLog>())).`when`(repository).getAllLogs()
    val vm = MainViewModel(repository, context)

    // Act
    vm.deleteEntry(42L)
    testDispatcher.scheduler.advanceUntilIdle()

    // Assert: deleteById chiamato, non delete(entity)
    org.mockito.kotlin.verify(repository).deleteById(42L)
    org.mockito.kotlin.verifyNoMoreInteractions(repository)  // delete(entity) NON chiamato
}

@Test
fun deleteCallLog_callsDeleteLog() = runTest {
    doReturn(flowOf(emptyList<BlockedEntry>())).`when`(repository).getAll()
    doReturn(flowOf(emptyList<BlockedCallLog>())).`when`(repository).getAllLogs()
    val vm = MainViewModel(repository, context)

    vm.deleteCallLog(99L)
    testDispatcher.scheduler.advanceUntilIdle()

    org.mockito.kotlin.verify(repository).deleteLog(99L)
}

@Test
fun importCsv_success_setsUserMessage() = runTest {
    doReturn(flowOf(emptyList<BlockedEntry>())).`when`(repository).getAll()
    doReturn(flowOf(emptyList<BlockedCallLog>())).`when`(repository).getAllLogs()
    val vm = MainViewModel(repository, context)

    val csv = "pattern,isPrefix,label,action\n+39333123456,false,Test,BLOCK\n".toByteArray()
    vm.importCsv(java.io.ByteArrayInputStream(csv))
    testDispatcher.scheduler.advanceUntilIdle()

    val message = vm.uiState.value.userMessage
    assert(message != null && message.contains("1")) {
        "userMessage should mention the count of imported entries, was: $message"
    }
}

@Test
fun clearUserMessage_clearsMessage() = runTest {
    doReturn(flowOf(emptyList<BlockedEntry>())).`when`(repository).getAll()
    doReturn(flowOf(emptyList<BlockedCallLog>())).`when`(repository).getAllLogs()
    val vm = MainViewModel(repository, context)

    val csv = "pattern,isPrefix,label,action\n+39333,false,Test,BLOCK\n".toByteArray()
    vm.importCsv(java.io.ByteArrayInputStream(csv))
    testDispatcher.scheduler.advanceUntilIdle()
    assert(vm.uiState.value.userMessage != null)

    vm.clearUserMessage()

    assertEquals(null, vm.uiState.value.userMessage)
}
```

Aggiungere anche l'import mancante in cima al file:
```kotlin
import com.callsblocker.data.BlockedCallLog
```
(già presente, verificare)

- [ ] **Step 2: Eseguire i test per verificare che falliscano**

```
.\gradlew.bat test --tests "com.callsblocker.ui.MainViewModelTest"
```
Expected: FAIL — metodi non trovati

- [ ] **Step 3: Aggiornare `UiState` aggiungendo `userMessage`**

In `MainViewModel.kt`, modificare `data class UiState`:

```kotlin
data class UiState(
    val entries: List<BlockedEntry> = emptyList(),
    val callLogs: List<BlockedCallLog> = emptyList(),
    val isScreeningActive: Boolean = false,
    val isBatteryOptimized: Boolean = false,
    val userMessage: String? = null   // ← nuovo campo
)
```

- [ ] **Step 4: Aggiornare `deleteEntry` per usare `deleteById`**

In `MainViewModel.kt`, sostituire il metodo `deleteEntry`:

```kotlin
fun deleteEntry(entryId: Long) {
    viewModelScope.launch {
        repository.deleteById(entryId)
    }
}
```

(Rimuovere il precedente scan con `_uiState.value.entries.find { it.id == entryId }`)

- [ ] **Step 5: Aggiungere `deleteCallLog`**

In `MainViewModel.kt`, aggiungere dopo `clearCallLogs()`:

```kotlin
fun deleteCallLog(logId: Long) {
    viewModelScope.launch {
        repository.deleteLog(logId)
    }
}
```

- [ ] **Step 6: Sostituire Toast con `userMessage` in `importCsv`**

In `MainViewModel.kt`, sostituire l'intero metodo `importCsv`:

```kotlin
fun importCsv(inputStream: InputStream) {
    viewModelScope.launch {
        try {
            val importedEntries = CsvManager.import(inputStream)
            if (importedEntries.isNotEmpty()) {
                repository.insertAll(importedEntries)
                _uiState.value = _uiState.value.copy(
                    userMessage = "Importati ${importedEntries.size} numeri"
                )
            } else {
                _uiState.value = _uiState.value.copy(
                    userMessage = "Nessun numero trovato nel file"
                )
            }
        } catch (e: Exception) {
            android.util.Log.e("MainViewModel", "Error importing entries", e)
            _uiState.value = _uiState.value.copy(
                userMessage = "Errore durante l'importazione"
            )
        }
    }
}
```

- [ ] **Step 7: Aggiungere `clearUserMessage`**

In `MainViewModel.kt`, aggiungere:

```kotlin
fun clearUserMessage() {
    _uiState.value = _uiState.value.copy(userMessage = null)
}
```

Rimuovere anche gli import non più usati:
```kotlin
// Rimuovere:
// import android.widget.Toast
```

- [ ] **Step 8: Eseguire i test**

```
.\gradlew.bat test --tests "com.callsblocker.ui.MainViewModelTest"
```
Expected: tutti PASS

- [ ] **Step 9: Commit**

```
git add app/src/main/java/com/callsblocker/ui/MainViewModel.kt
git add app/src/test/java/com/callsblocker/ui/MainViewModelTest.kt
git commit -m "fix(viewmodel): replace Toast with userMessage state, use deleteById, add deleteCallLog"
```

---

## Task 3 — Fix: ViewModel condiviso a scope Activity (stale `isScreeningActive`)

**Files:**
- Modify: `app/src/main/java/com/callsblocker/ui/MainActivity.kt`
- Modify: `app/src/main/java/com/callsblocker/ui/screens/HomeScreen.kt`

**Bug 2**: ogni destinazione nav aveva la propria istanza di `MainViewModel`. `isScreeningActive` (non proveniente da DB) non veniva aggiornato nella Home dopo aver concesso il ruolo dalle Impostazioni.

- [ ] **Step 1: Spostare `hiltViewModel<MainViewModel>()` fuori dal NavHost in `MainActivity`**

In `MainActivity.kt`, modificare `setMainContent()`:

```kotlin
private fun setMainContent() {
    setContent {
        CallsBlockerTheme {
            val navController = rememberNavController()
            // ViewModel creato UNA volta, scoped all'Activity, condiviso tra tutte le schermate
            val sharedViewModel = hiltViewModel<MainViewModel>()
            NavHost(
                navController = navController,
                startDestination = "home"
            ) {
                composable("home") {
                    HomeScreen(
                        viewModel = sharedViewModel,
                        navController = navController
                    )
                }
                composable("settings") {
                    SettingsScreen(
                        navController = navController,
                        viewModel = sharedViewModel,
                        roleManager = roleManager,
                        batteryManager = batteryManager
                    )
                }
                composable("info") {
                    InfoScreen(navController = navController)
                }
                composable("logs") {
                    BlockedCallsScreen(
                        viewModel = sharedViewModel,
                        navController = navController
                    )
                }
            }
        }
    }
}
```

Rimuovere anche gli import inutilizzati (Bug 11):
```kotlin
// Rimuovere queste righe (non usate in MainActivity):
// import androidx.compose.runtime.collectAsState
// import androidx.compose.runtime.getValue
```

- [ ] **Step 2: Aggiungere `LaunchedEffect` per refresh status in `HomeScreen`**

In `HomeScreen.kt`, aggiungere dentro la funzione `HomeScreen`, prima del `Scaffold`:

```kotlin
// Aggiorna lo stato di screening ogni volta che la home torna in foreground
val lifecycleOwner = androidx.lifecycle.compose.LocalLifecycleOwner.current
androidx.compose.runtime.LaunchedEffect(lifecycleOwner) {
    lifecycleOwner.lifecycle.addObserver(object : androidx.lifecycle.DefaultLifecycleObserver {
        override fun onResume(owner: androidx.lifecycle.LifecycleOwner) {
            viewModel.updateScreeningStatus()
        }
    })
}
```

Aggiungere gli import necessari in cima a `HomeScreen.kt`:
```kotlin
import androidx.lifecycle.DefaultLifecycleObserver
import androidx.lifecycle.LifecycleOwner
import androidx.lifecycle.compose.LocalLifecycleOwner
```

- [ ] **Step 3: Build**

```
.\gradlew.bat assembleDebug
```
Expected: `BUILD SUCCESSFUL`

- [ ] **Step 4: Commit**

```
git add app/src/main/java/com/callsblocker/ui/MainActivity.kt
git add app/src/main/java/com/callsblocker/ui/screens/HomeScreen.kt
git commit -m "fix(nav): share MainViewModel at Activity scope so isScreeningActive stays fresh"
```

---

## Task 4 — Fix: Toggle notifiche stale in `SettingsScreen`

**Files:**
- Modify: `app/src/main/java/com/callsblocker/ui/screens/SettingsScreen.kt`
- Modify: `app/src/androidTest/java/com/callsblocker/ui/screens/SettingsScreenTest.kt`

**Bug 1**: `notificationsChecked` era inizializzato con `remember { mutableStateOf(notificationsEnabled) }`, che cattura il valore `initial = true` e non si aggiorna mai quando DataStore carica il valore reale.

- [ ] **Step 1: Scrivere il test failing**

In `SettingsScreenTest.kt`, aggiungere:

```kotlin
@Test
fun settingsScreen_notificationsOff_switchShowsOff() {
    val viewModel = mockViewModel(isActive = true, isOptimized = false)
    // Simula DataStore che restituisce notifiche disattivate
    val prefsManager = mockk<PrefsManager>(relaxed = true) {
        every { notificationsEnabled } returns flowOf(false)
    }
    composeTestRule.setContent {
        CallsBlockerTheme {
            val navController = rememberNavController()
            SettingsScreen(
                navController = navController,
                viewModel = viewModel,
                roleManager = mockk(relaxed = true),
                batteryManager = mockk(relaxed = true)
            )
        }
    }
    // Il toggle deve essere in stato OFF
    composeTestRule.onNode(
        androidx.compose.ui.test.hasText("Notifica quando una chiamata viene bloccata")
            .and(androidx.compose.ui.test.hasAnyDescendant(
                androidx.compose.ui.test.SemanticsMatcher.expectValue(
                    androidx.compose.ui.semantics.SemanticsProperties.ToggleableState,
                    androidx.compose.ui.state.ToggleableState.Off
                )
            ))
    ).assertExists()
}
```

> Nota: SettingsScreen crea `PrefsManager` internamente con `remember { PrefsManager(context) }`, quindi non è iniettabile nel test standard. Il test funzionale completo per questo bug è meglio fatto verificando l'assenza del doppio-stato mediante ispezione del codice. Aggiungere invece questo test di comportamento UI che usa lo stato iniziale reale:

```kotlin
@Test
fun settingsScreen_notificationsSwitch_isPresent() {
    val viewModel = mockViewModel(isActive = true, isOptimized = false)
    setScreen(viewModel)
    // Verificare che lo switch notifiche esiste
    composeTestRule.onNodeWithText("Notifica quando una chiamata viene bloccata")
        .assertIsDisplayed()
}
```

- [ ] **Step 2: Eseguire il test (solo per verifica display)**

```
.\gradlew.bat connectedAndroidTest --tests "com.callsblocker.ui.screens.SettingsScreenTest.settingsScreen_notificationsSwitch_isPresent"
```
Expected: PASS (il test di presenza passa già)

- [ ] **Step 3: Fix del bug — rimuovere `notificationsChecked`, usare `notificationsEnabled` direttamente**

In `SettingsScreen.kt`, trovare e rimuovere:

```kotlin
// RIMUOVERE questa riga:
var notificationsChecked by remember { mutableStateOf(notificationsEnabled) }
```

Trovare lo `Switch` e modificarlo per usare `notificationsEnabled` direttamente:

```kotlin
// PRIMA:
Switch(
    checked = notificationsChecked,
    onCheckedChange = { newValue ->
        notificationsChecked = newValue
        scope.launch {
            prefsManager.setNotificationsEnabled(newValue)
        }
    }
)

// DOPO:
Switch(
    checked = notificationsEnabled,
    onCheckedChange = { newValue ->
        scope.launch {
            prefsManager.setNotificationsEnabled(newValue)
        }
    }
)
```

Rimuovere anche l'import `androidx.compose.runtime.setValue` se non usato altrove in SettingsScreen.

- [ ] **Step 4: Build**

```
.\gradlew.bat assembleDebug
```
Expected: `BUILD SUCCESSFUL`

- [ ] **Step 5: Commit**

```
git add app/src/main/java/com/callsblocker/ui/screens/SettingsScreen.kt
git add app/src/androidTest/java/com/callsblocker/ui/screens/SettingsScreenTest.kt
git commit -m "fix(settings): remove stale notificationsChecked, drive switch directly from DataStore"
```

---

## Task 5 — Fix: Icona notifica placeholder in `NotificationHelper`

**Files:**
- Modify: `app/src/main/java/com/callsblocker/util/NotificationHelper.kt`

**Bug 3**: `android.R.drawable.sym_def_app_icon` è un'icona di sistema generica (il "bug Android"). Il progetto usa `@mipmap/ic_launcher_foreground` come foreground dell'adaptive icon.

- [ ] **Step 1: Sostituire l'icona in `NotificationHelper.kt`**

In `NotificationHelper.kt`, aggiungere l'import:
```kotlin
import com.callsblocker.R
```
(probabilmente già presente, verificare)

Trovare e modificare la riga con `setSmallIcon`:

```kotlin
// PRIMA:
.setSmallIcon(android.R.drawable.sym_def_app_icon)

// DOPO:
.setSmallIcon(R.mipmap.ic_launcher_foreground)
```

- [ ] **Step 2: Build**

```
.\gradlew.bat assembleDebug
```
Expected: `BUILD SUCCESSFUL`

- [ ] **Step 3: Commit**

```
git add app/src/main/java/com/callsblocker/util/NotificationHelper.kt
git commit -m "fix(notification): use app icon instead of system placeholder"
```

---

## Task 6 — Fix: Pulsante Indietro in `InfoScreen`

**Files:**
- Modify: `app/src/main/java/com/callsblocker/ui/screens/InfoScreen.kt`
- Modify: `app/src/androidTest/java/com/callsblocker/ui/screens/InfoScreenTest.kt`

**Bug 5**: `navController` viene ricevuto ma non usato. Non c'è navigazione indietro dall'app bar.

- [ ] **Step 1: Scrivere il test failing**

In `InfoScreenTest.kt`, aggiungere:

```kotlin
@Test
fun infoScreen_hasBackButton() {
    composeTestRule.setContent {
        CallsBlockerTheme {
            val navController = rememberNavController()
            InfoScreen(navController = navController)
        }
    }

    composeTestRule.onNodeWithContentDescription("Indietro").assertIsDisplayed()
}
```

Aggiungere l'import:
```kotlin
import androidx.compose.ui.test.assertIsDisplayed
```

- [ ] **Step 2: Eseguire il test per verificare che fallisca**

```
.\gradlew.bat connectedAndroidTest --tests "com.callsblocker.ui.screens.InfoScreenTest.infoScreen_hasBackButton"
```
Expected: FAIL — il nodo con contentDescription "Indietro" non esiste

- [ ] **Step 3: Aggiungere il pulsante indietro in `InfoScreen.kt`**

In `InfoScreen.kt`, aggiungere i seguenti import:

```kotlin
import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.automirrored.filled.ArrowBack
import androidx.compose.material3.Icon
import androidx.compose.material3.IconButton
```

Modificare il `TopAppBar` dentro `InfoScreen`:

```kotlin
// PRIMA:
TopAppBar(title = { Text("Info") })

// DOPO:
TopAppBar(
    title = { Text("Info") },
    navigationIcon = {
        IconButton(onClick = { navController.popBackStack() }) {
            Icon(
                imageVector = Icons.AutoMirrored.Filled.ArrowBack,
                contentDescription = "Indietro"
            )
        }
    }
)
```

- [ ] **Step 4: Eseguire il test**

```
.\gradlew.bat connectedAndroidTest --tests "com.callsblocker.ui.screens.InfoScreenTest.infoScreen_hasBackButton"
```
Expected: PASS

- [ ] **Step 5: Commit**

```
git add app/src/main/java/com/callsblocker/ui/screens/InfoScreen.kt
git add app/src/androidTest/java/com/callsblocker/ui/screens/InfoScreenTest.kt
git commit -m "fix(ui): add back navigation button to InfoScreen"
```

---

## Task 7 — Fix: Delete singolo log + Snackbar import feedback in `HomeScreen`

**Files:**
- Modify: `app/src/main/java/com/callsblocker/ui/screens/BlockedCallsScreen.kt`
- Modify: `app/src/main/java/com/callsblocker/ui/screens/HomeScreen.kt`
- Create: `app/src/androidTest/java/com/callsblocker/ui/screens/BlockedCallsScreenTest.kt`

**Bug 4 (UI)**: `BlockedCallsScreen` mostra solo il delete all. Ogni `CallLogItem` non ha un pulsante di cancellazione individuale.  
**Bug 6 (UI)**: `HomeScreen` deve mostrare lo `Snackbar` quando `uiState.userMessage != null`.

- [ ] **Step 1: Creare il test per `BlockedCallsScreen`**

Creare il file `app/src/androidTest/java/com/callsblocker/ui/screens/BlockedCallsScreenTest.kt`:

```kotlin
package com.callsblocker.ui.screens

import androidx.compose.ui.test.assertIsDisplayed
import androidx.compose.ui.test.junit4.createComposeRule
import androidx.compose.ui.test.onNodeWithContentDescription
import androidx.compose.ui.test.onNodeWithText
import androidx.compose.ui.test.performClick
import androidx.test.ext.junit.runners.AndroidJUnit4
import com.callsblocker.data.BlockedCallLog
import com.callsblocker.data.CallAction
import com.callsblocker.ui.MainViewModel
import com.callsblocker.ui.UiState
import com.callsblocker.ui.theme.CallsBlockerTheme
import io.mockk.every
import io.mockk.mockk
import io.mockk.verify
import kotlinx.coroutines.flow.MutableStateFlow
import org.junit.Rule
import org.junit.Test
import org.junit.runner.RunWith
import androidx.navigation.compose.rememberNavController

@RunWith(AndroidJUnit4::class)
class BlockedCallsScreenTest {

    @get:Rule
    val composeTestRule = createComposeRule()

    private val sampleLog = BlockedCallLog(
        id = 1L,
        number = "+39333123456",
        timestamp = 1714489200000L, // 2024-04-30
        action = CallAction.BLOCK,
        label = "Spam"
    )

    private fun buildViewModel(logs: List<BlockedCallLog>): MainViewModel {
        return mockk<MainViewModel>(relaxed = true) {
            every { uiState } returns MutableStateFlow(
                UiState(callLogs = logs)
            )
        }
    }

    @Test
    fun blockedCallsScreen_showsLogEntry() {
        val viewModel = buildViewModel(listOf(sampleLog))
        composeTestRule.setContent {
            CallsBlockerTheme {
                BlockedCallsScreen(viewModel = viewModel, navController = rememberNavController())
            }
        }
        composeTestRule.onNodeWithText("+39333123456").assertIsDisplayed()
        composeTestRule.onNodeWithText("Bloccata").assertIsDisplayed()
    }

    @Test
    fun blockedCallsScreen_emptyState_showsMessage() {
        val viewModel = buildViewModel(emptyList())
        composeTestRule.setContent {
            CallsBlockerTheme {
                BlockedCallsScreen(viewModel = viewModel, navController = rememberNavController())
            }
        }
        composeTestRule.onNodeWithText("Nessuna chiamata intercettata").assertIsDisplayed()
    }

    @Test
    fun blockedCallsScreen_deleteButton_callsDeleteCallLog() {
        val viewModel = buildViewModel(listOf(sampleLog))
        composeTestRule.setContent {
            CallsBlockerTheme {
                BlockedCallsScreen(viewModel = viewModel, navController = rememberNavController())
            }
        }
        // Il pulsante di delete per il singolo log deve essere presente
        composeTestRule.onNodeWithContentDescription("Elimina log").assertIsDisplayed()
        composeTestRule.onNodeWithContentDescription("Elimina log").performClick()
        // Verifica che deleteCallLog sia stato chiamato con l'ID corretto
        verify { viewModel.deleteCallLog(1L) }
    }

    @Test
    fun blockedCallsScreen_deleteAllButton_callsClearCallLogs() {
        val viewModel = buildViewModel(listOf(sampleLog))
        composeTestRule.setContent {
            CallsBlockerTheme {
                BlockedCallsScreen(viewModel = viewModel, navController = rememberNavController())
            }
        }
        composeTestRule.onNodeWithContentDescription("Cancella tutto").performClick()
        verify { viewModel.clearCallLogs() }
    }
}
```

- [ ] **Step 2: Eseguire i test per verificare che `deleteButton_callsDeleteCallLog` fallisca**

```
.\gradlew.bat connectedAndroidTest --tests "com.callsblocker.ui.screens.BlockedCallsScreenTest"
```
Expected: `deleteButton_callsDeleteCallLog` FAIL (il pulsante non esiste ancora)

- [ ] **Step 3: Aggiungere il pulsante delete in `CallLogItem` e collegarlo**

In `BlockedCallsScreen.kt`, modificare la signature di `CallLogItem` e aggiungere il pulsante:

```kotlin
// PRIMA:
@Composable
fun CallLogItem(log: BlockedCallLog) {
    val dateFormat = SimpleDateFormat("dd MMM, HH:mm", Locale.getDefault())

// DOPO:
@Composable
fun CallLogItem(
    log: BlockedCallLog,
    onDelete: (Long) -> Unit = {}
) {
    val dateFormat = remember { SimpleDateFormat("dd MMM, HH:mm", Locale.getDefault()) }
```

> **Nota:** il `remember { }` intorno a `SimpleDateFormat` risolve anche il **Bug 8** (ricreazione ad ogni recomposition).

Aggiungere l'import:
```kotlin
import androidx.compose.material.icons.filled.Delete
import androidx.compose.material3.IconButton
import androidx.compose.material3.Icon
```

Nella `Row` di `CallLogItem`, aggiungere il pulsante delete dopo il testo dell'azione:

```kotlin
// Aggiungere DOPO il Text(actionText):
IconButton(onClick = { onDelete(log.id) }) {
    Icon(
        imageVector = Icons.Filled.Delete,
        contentDescription = "Elimina log",
        tint = MaterialTheme.colorScheme.onSurfaceVariant.copy(alpha = 0.6f)
    )
}
```

In `BlockedCallsScreen`, aggiornare la chiamata a `CallLogItem`:

```kotlin
// PRIMA:
items(uiState.callLogs) { log ->
    CallLogItem(log)
}

// DOPO:
items(uiState.callLogs, key = { it.id }) { log ->
    CallLogItem(
        log = log,
        onDelete = { viewModel.deleteCallLog(it) }
    )
}
```

- [ ] **Step 4: Aggiungere Snackbar per `userMessage` in `HomeScreen`**

In `HomeScreen.kt`, modificare la firma e il body per gestire Snackbar:

Aggiungere import:
```kotlin
import androidx.compose.material3.SnackbarHost
import androidx.compose.material3.SnackbarHostState
import androidx.compose.runtime.LaunchedEffect
import androidx.compose.runtime.remember
```

Dentro `HomeScreen`, aggiungere prima del Scaffold:
```kotlin
val snackbarHostState = remember { SnackbarHostState() }
val userMessage = uiState.userMessage

LaunchedEffect(userMessage) {
    if (userMessage != null) {
        snackbarHostState.showSnackbar(userMessage)
        viewModel.clearUserMessage()
    }
}
```

Modificare lo `Scaffold` per aggiungere `snackbarHost`:
```kotlin
Scaffold(
    topBar = { /* ... esistente ... */ },
    floatingActionButton = { /* ... esistente ... */ },
    snackbarHost = { SnackbarHost(snackbarHostState) }   // ← aggiungere
) { paddingValues ->
    // ... resto invariato
}
```

- [ ] **Step 5: Eseguire i test**

```
.\gradlew.bat connectedAndroidTest --tests "com.callsblocker.ui.screens.BlockedCallsScreenTest"
```
Expected: tutti PASS

- [ ] **Step 6: Commit**

```
git add app/src/main/java/com/callsblocker/ui/screens/BlockedCallsScreen.kt
git add app/src/main/java/com/callsblocker/ui/screens/HomeScreen.kt
git add app/src/androidTest/java/com/callsblocker/ui/screens/BlockedCallsScreenTest.kt
git commit -m "fix(ui): add per-item log delete, Snackbar for import feedback, fix SimpleDateFormat recomposition"
```

---

## Task 8 — Fix: Doppio inset tastiera in `EntryBottomSheet`

**Files:**
- Modify: `app/src/main/java/com/callsblocker/ui/components/EntryBottomSheet.kt`
- Modify: `app/src/androidTest/java/com/callsblocker/ui/components/EntryBottomSheetTest.kt`

**Bug 9**: `ModalBottomSheet` applica di default i window insets. `imePadding()` nel `Column` interno aggiunge un secondo strato di padding per la tastiera. Risultato: la tastiera crea un gap eccessivo sotto il sheet.

- [ ] **Step 1: Verificare il test esistente**

```
.\gradlew.bat connectedAndroidTest --tests "com.callsblocker.ui.components.EntryBottomSheetTest"
```
Expected: PASS (i test esistenti passano prima della modifica)

- [ ] **Step 2: Aggiungere `windowInsets = WindowInsets(0)` al `ModalBottomSheet`**

In `EntryBottomSheet.kt`, modificare il `ModalBottomSheet`:

```kotlin
// PRIMA:
ModalBottomSheet(
    onDismissRequest = onDismiss,
    modifier = Modifier.fillMaxWidth(),
    containerColor = MaterialTheme.colorScheme.surface,
    tonalElevation = 8.dp
) {

// DOPO:
ModalBottomSheet(
    onDismissRequest = onDismiss,
    modifier = Modifier.fillMaxWidth(),
    containerColor = MaterialTheme.colorScheme.surface,
    tonalElevation = 8.dp,
    windowInsets = WindowInsets(0)   // ← evita doppio inset con imePadding()
) {
```

`WindowInsets` è già importato in cima al file (`import androidx.compose.foundation.layout.WindowInsets`).

- [ ] **Step 3: Build**

```
.\gradlew.bat assembleDebug
```
Expected: `BUILD SUCCESSFUL`

- [ ] **Step 4: Eseguire i test esistenti per verifica regressione**

```
.\gradlew.bat connectedAndroidTest --tests "com.callsblocker.ui.components.EntryBottomSheetTest"
```
Expected: PASS (nessuna regressione)

- [ ] **Step 5: Commit**

```
git add app/src/main/java/com/callsblocker/ui/components/EntryBottomSheet.kt
git commit -m "fix(ui): set windowInsets=0 on ModalBottomSheet to avoid double keyboard padding"
```

---

## Task 9 — Cleanup: `OnboardingViewModel` e import inutilizzati

**Files:**
- Modify: `app/src/main/java/com/callsblocker/ui/OnboardingViewModel.kt`

**Bug 10**: `@ApplicationContext private val context: Context` è iniettato ma non usato nel corpo del ViewModel.

- [ ] **Step 1: Eseguire il test esistente come baseline**

```
.\gradlew.bat test --tests "com.callsblocker.ui.OnboardingViewModelTest"
```
Expected: PASS

- [ ] **Step 2: Rimuovere il parametro `context` da `OnboardingViewModel`**

In `OnboardingViewModel.kt`, modificare il costruttore:

```kotlin
// PRIMA:
class OnboardingViewModel @Inject constructor(
    private val prefsManager: PrefsManager,
    @ApplicationContext private val context: Context
) : ViewModel() {

// DOPO:
class OnboardingViewModel @Inject constructor(
    private val prefsManager: PrefsManager
) : ViewModel() {
```

Rimuovere i seguenti import non più necessari:
```kotlin
// Rimuovere:
// import android.content.Context
// import dagger.hilt.android.qualifiers.ApplicationContext
```

- [ ] **Step 3: Build + test**

```
.\gradlew.bat assembleDebug test --tests "com.callsblocker.ui.OnboardingViewModelTest"
```
Expected: `BUILD SUCCESSFUL`, test PASS

- [ ] **Step 4: Commit**

```
git add app/src/main/java/com/callsblocker/ui/OnboardingViewModel.kt
git commit -m "chore: remove unused context injection from OnboardingViewModel"
```

---

## Task 10 — Run finale e verifica

- [ ] **Step 1: Build completo**

```
.\gradlew.bat assembleDebug
```
Expected: `BUILD SUCCESSFUL` senza errori

- [ ] **Step 2: Test unitari completi**

```
.\gradlew.bat test
```
Expected: `BUILD SUCCESSFUL`, tutti i test PASS

- [ ] **Step 3: Verifica check lint**

```
.\gradlew.bat check
```
Expected: `BUILD SUCCESSFUL`

- [ ] **Step 4: Commit finale (se presenti modifiche non committate)**

```
git log --oneline -10
```
Verificare che tutti i commit dei task precedenti siano presenti.

---

## Self-Review

### Copertura spec

| Bug | Task | Coperto? |
|-----|------|----------|
| 1 - Toggle notifiche stale | Task 4 | ✅ |
| 2 - isScreeningActive stale | Task 3 | ✅ |
| 3 - Icona notifica placeholder | Task 5 | ✅ |
| 4 - No delete singolo log | Task 1 + 2 + 7 | ✅ |
| 5 - InfoScreen senza back button | Task 6 | ✅ |
| 6 - Toast da ViewModel | Task 2 + 7 | ✅ |
| 7 - deleteEntry O(n) | Task 1 + 2 | ✅ |
| 8 - SimpleDateFormat per recomposition | Task 7 (incluso in `CallLogItem`) | ✅ |
| 9 - ModalBottomSheet doppio inset | Task 8 | ✅ |
| 10 - context inutilizzato in OnboardingViewModel | Task 9 | ✅ |
| 11 - Import inutilizzati MainActivity | Task 3 | ✅ |

### Placeholder scan
Nessun placeholder trovato. Ogni step contiene codice completo.

### Type consistency
- `deleteById(id: Long)`: definito in Task 1 (DAO + Repository), usato in Task 2 (ViewModel) ✅
- `deleteLog(id: Long)`: definito in Task 1 (DAO + Repository), usato in Task 2 (ViewModel), Task 7 (UI) ✅
- `deleteCallLog(logId: Long)`: definito in Task 2 (ViewModel), usato in Task 7 (UI) ✅
- `userMessage: String?`: definito in Task 2 (UiState), usato in Task 7 (HomeScreen Snackbar) ✅
- `clearUserMessage()`: definito in Task 2 (ViewModel), usato in Task 7 (HomeScreen LaunchedEffect) ✅
